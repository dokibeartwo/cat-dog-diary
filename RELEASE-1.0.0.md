# 猫狗日记 1.0.0 发布说明

在 0.5.4 的六主题、透明小组件、全屏专注、习惯与离席提醒基础上，集中改善记录、安排、提醒和数据恢复。

### 2026-09-17 界面修订

- “切换主题”和“暂时离开”恢复为左下角 280px 宽的小弹层，保留原来的缩略图、圆角与两列布局；跟随按钮定位并避让窗口边缘，不再被撑成横跨整个窗口的大面板。
- 阶段起止日期及箭头组成同一组，箭头与输入框垂直居中、左右间距一致；窄窗口整组换行，不拆散日期范围。
- 设置、主列表、侧栏和弹层的滚动条统一为细窄的主题色，不再出现突兀的系统白色轨道。
- 本次只修正布局，版本仍为 1.0.0，不改变任务、提醒和专注数据。

## 新增与改进

1. 今天工作台分组、今日重点、待安排入口、分类筛选和紧凑专注区；保留之前未完成事项。
2. 快捷记录和明确的中文日期／时间识别，先预览确认再保存；支持只安排日期，不必制造具体执行时间。
3. 提醒中心、轻提示与全屏选择、提醒总开关、勿扰时段、可配置稍后时长、暂缓原因和处理记录。
4. 每日坚持的星期与时段设置；新用户示例习惯默认暂停。
5. 完成后生成下一期的日／工作日／周／月重复任务，与循环检查区分。
6. 根据截止、优先级和预计分钟进行容量排期，先预览再应用，保护手动日期并列出容量冲突。
7. 本地备份、导入导出、恢复预览和任务回收站；读取失败不静默覆盖原数据。
8. 统一稍后提醒、日期、时间、数字、分类建议、选择项和确认框的主题 UI。
9. Electron 运行时更新为 44.4.1，使用锁定依赖与项目内资源编辑器；候选包完成后再切换，旧发布包默认保留。

## 兼容与数据

- 使用原用户数据目录，旧任务、习惯、主题、计时及统计补充默认字段后读取。
- 原背景、屏保和小熊素材不变；六主题小组件保持 420×590 逻辑像素。
- 没有账号、云同步、联网 AI 或自动上传。导入是替换，不是合并，确认前展示概要并备份现有数据。
- 回退旧 EXE 不等于回退新版数据；确需回退时，应退出后使用升级前备份，不直接让旧版覆盖新结构。

## 交付与限制

交付 Windows x64 便携文件夹、完整 ZIP 和 SHA-256 校验文件。桌面快捷方式应指向已部署的新 EXE，而不是开发目录；迁移位置后自启动需重新开启。

本说明描述实现范围，不代替每次构建的验收报告，也不声称未现场复现的问题永久消失。打包版通过隔离验收后再部署；实际 Windows 注销／重启验证由用户方便时进行。

当前没有代码签名证书、自动更新、安装器、云同步或多设备合并。安全软件是否放行不能保证，不建议关闭防护。现有素材的公开再分发权利仍需发布者核实；版本标记 1.0.0 或代码标记 MIT 不授予素材权利。
