# 猫狗日记：离线内测先读我

Windows 1.1.0-beta.3 / Android 0.1.1。此次先测试本机任务、习惯、专注和提醒，**没有启用云同步，也不需要注册账号**。两端数据目前独立，不会互相出现。

## 文件和数据放在哪里

推荐在 E 盘按下面结构放置。Windows 程序必须保留完整文件夹，不能只拿走 EXE。

```text
E:\猫狗日记\离线内测\
  猫狗日记-windows-x64\     Windows 程序（双击其中的猫狗日记.exe）
  本地数据\                首次启动后生成：任务、SQLite、每日备份
  安装包\                  Windows ZIP、Android APK、校验值
  离线内测-先读我.md
```

正式版及原快捷方式不变。内测版初始为空是正常现象；如要试用原有任务，请在正式版“设置与备份”导出，再在内测版“导入备份”查看预览、确认导入。没有经过确认不会搬走原任务。

“本地数据”包含私人任务，不要一起发送给朋友、不要上传 GitHub。朋友只需安装包即可。移动整个离线内测文件夹时，先退出程序，并带上本地数据；不要仅移动程序而漏掉数据。

安卓数据库保存在手机自己的应用私有目录，不在电脑 E 盘。手机“设置 → 导出本机数据”可保存一份 JSON 到自己选的位置。这个手机备份格式目前不是 Windows 的直接导入格式；本阶段不以文件互拷冒充同步。内测数据请保留额外副本，不把 APK 当作重要任务的唯一保存位置。

## 安装 Android APK

1. 将 `cat-dog-diary-android-0.1.1-internal.apk` 发到安卓手机，点击安装；仅对你使用的文件管理器临时允许安装来源，不需要关闭杀毒或系统保护。
2. 打开后可直接记任务、添加每日坚持、开始专注；无需输入邮箱。
3. “设置 → 允许醒目通知”中允许通知。需要精确到点时再打开“闹钟和提醒权限”。不授予也能管理任务，提醒可能受限。
4. 同一签名的内测包可直接覆盖升级。若出现签名不兼容，不要直接卸载：先导出数据、反馈具体提示。当前是调试签名内测包，公开正式发行前还需要长期签名。

截图中的邮件来自 GitHub Actions 构建通知，不是邮箱验证码或数据库故障。工作流目前只有手动触发；失败说明构建或安装测试没有通过，不代表邮件系统异常。此次没有修改你的邮件通知设置，也没有删除失败记录。

## 建议你在手机上实际试这几项

- 关闭 Wi-Fi 和移动网络，新增、编辑、完成任务；关闭应用再打开，确认内容仍在。
- 添加一个 1 分钟循环提醒，不处理它，观察是否只保留一条；处理后下一次从处理时重新计时。
- 分别在应用前台、锁屏和切到其他应用时测试提醒。前台有主题全屏提醒；后台是通知、震动与锁屏提示，不能保证强制全屏。
- 专注开始后暂停、锁屏、重新打开；检查剩余时间与记录。中途结束不增加完成轮数。
- 切换六套主题，检查大字、按钮、日期／时间以及屏幕底部是否被遮挡。
- 关闭通知权限、震动，分别验证实际效果。厂商省电、“强制停止”和清理后台可能影响提醒；不要把模拟器通过等同于真机通过。

反馈时给出手机型号、Android 版本、操作步骤和不含私人任务的截图即可。不要公开完整数据库、导出文件、验证码或密钥。

## 下一阶段

离线使用反馈稳定后，再创建 Supabase 项目，部署权限隔离和同步接口，联调邮箱验证码与电脑↔手机同步。云端数据库届时存于所选 Supabase 项目；E 盘和手机仍保留本地副本。
