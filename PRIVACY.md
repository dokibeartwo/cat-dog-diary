# 隐私与数据说明

猫狗日记 Windows 1.0.0 默认是本地应用，无广告、遥测、联网 AI 或自动上传功能。同步内测为用户主动开启的可选功能；没有配置 Supabase 或没有登录时，正常任务管理和提醒不需要网络。

## 本地保存

默认数据位于 `%APPDATA%\猫狗日记\done-data.json`。内容包括任务及备注、小步骤、分类、计划／截止时间、习惯、主题与提醒偏好、离席状态、专注记录、待处理提醒、处理记录及回收站。备份位于同目录的 backups，导出文件路径由用户选择。

1.1.0-beta.3 内测包通过 EXE 旁的 `data-location.json` 使用独立的兄弟目录 `本地数据`。把完整程序文件夹解压到 E 盘，即在 E 盘保存，不自动读取或覆盖正式版 AppData。设置页会显示实际路径。

Windows 内测版还会在同一用户数据目录保存 `sync-state.sqlite`，其中包含设备编号、同步游标、待上传变更和本地冲突记录；旧版 `sync-state.json` 仅作为迁移入口保留。登录会话在系统加密可用时用 Electron safeStorage 加密保存，任务正文未加密。当前离线包未配置 Supabase、不要求登录；只有用户以后主动配置、登录并确认合并，任务、账号级主题和专注历史才会上传。窗口位置、小组件可见性、离席状态、当前提醒弹窗、通知编号和正在运行的专注界面只留在设备本地。

JSON 未加密；能访问当前 Windows 用户文件的人可能读取。自动备份位于同一磁盘，不代替异地备份。导出、恢复和迁移由用户主动执行；恢复前保存原数据。

本地窗口诊断日志记录生命周期、显示模式和错误分类，不记录任务正文。应用不自动发送日志。截图、日志和导出仍应在分享前检查。

## Windows 集成

全局快捷键只注册指定组合以唤起记录入口，不进行通用键盘录制。自启动在用户开启时注册当前电脑上的 EXE 路径；开发和隔离 QA 不注册。通知、托盘与安全提示由 Windows 管理。

## Android 内测

Android 0.1.1 当前为离线内测，不需要账号，数据存于手机应用私有目录中的 `cat-dog-diary.db`。导出操作通过系统分享保存 JSON；导出文件包含任务正文，不包含 SecureStore 中的登录令牌。卸载或清除应用数据会删除本机数据库，请先导出。

后续云同步采用邮箱验证码登录，维护者须先创建 Supabase 项目并执行全部迁移。应用由用户主动允许通知，并提供震动和精确闹钟设置；锁屏显示由通知频道与系统控制，不请求强制后台全屏权限。后台提醒受 Android 厂商和省电策略影响，不能保证强制全屏。无 Google 服务设备可使用本地排程，首版不依赖 FCM。

Supabase 表启用 Row Level Security，普通客户端只拿 publishable/anon key；service-role key 不进入 APK、Windows EXE 或仓库。用户可以在应用设置导出本机数据、退出账号或调用删除云端账号；删除后公开同步表通过外键级联清除，但本机 JSON 和备份不会自动删除。

## 删除与反馈

退出并关闭自启动后可删除程序文件，任务数据不会自动删除。确认不再需要后可自行删除 AppData 数据目录，建议先导出备份。提交 GitHub 问题或分享压缩包时，不要包含真实任务、私人截图与个人备份。
